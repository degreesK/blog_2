<%= link_to 'New', '/posts/new' %>
<table>
  <% @posts.each do |post| %>
  <tr><td>
  <%= link_to post.title, "/posts/#{post.id}" %></td><td><%=
  button_to 'Delete', "/posts/#{post.id}", method: 'delete' %>
  </td></tr>
  <% end %>
</table>
