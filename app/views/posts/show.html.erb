<h3><%= @post.title %></h3>
<p>
    <%= @post.body %>
</p>
<ul>
<% @post.categories.each do |category| %>
  <li><%= link_to category.name, category %></li>
<% end %>
</ul>

<%= link_to 'Back', '/posts' %>&nbsp; &nbsp;<%= link_to 'Edit',
"/posts/#{@post.id}/edit" %>
