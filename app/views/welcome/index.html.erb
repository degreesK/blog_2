<h1>My Blog</h1>
<% @posts.each do |post| %>
  <h2><%= link_to post.title, post %></h2>
  <p>
    (<%= post.categories.map(&:name).join ', '%>)
  <!-- similar to line above...(<%= post.categories.map{|x| x.name}.join ', '%>) -->
  </p>
  <p>
    <%= post.body %>
  </p>
<% end %>
