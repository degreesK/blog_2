<h1>Categories</h1>
<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.name, category %><%= button_to 'Delete', category, method: :delete, data: { confirm: 'Are you sure?' } %> </li>
<% end %>
</ul>
<%= link_to "New Category", "/categories/new" %>
